# Get Alabama system (district) lookup table

Returns a data frame mapping system codes to system names. Alabama has
approximately 140 school systems (67 county, 73 city).

## Usage

``` r
get_system_lookup()
```

## Value

Data frame with system_code and system_name columns
