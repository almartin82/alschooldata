# Ensure all aggregation levels exist

Creates district and state aggregates if they don't exist in the data.

## Usage

``` r
ensure_aggregation_levels(df, end_year)
```

## Arguments

- df:

  Processed data frame

- end_year:

  School year end

## Value

Data frame with all aggregation levels
