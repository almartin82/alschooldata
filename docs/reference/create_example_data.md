# Create example enrollment data for vignettes

Returns a minimal dataset for vignette building during CI. Based on real
enrollment patterns from Alabama DOE data.

## Usage

``` r
create_example_data()
```

## Value

A data frame with example enrollment data
